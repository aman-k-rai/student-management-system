version: '3.8'

services:
  mysql:
    image: mysql:8
    container_name: mysql-container
    environment:
      MYSQL_ROOT_PASSWORD: sql123
      MYSQL_DATABASE: studentdb
    ports:
      - "3307:3306"
    volumes:
      - ./db/studentdb.sql:/docker-entrypoint-initdb.d/studentdb.sql

  flask:
    image: amanrai3011/student-management-app:latest
    container_name: student-app
    environment:
      MYSQL_ROOT_PASSWORD: sql123
    ports:
      - "5000:5000"
    depends_on:
      - mysql